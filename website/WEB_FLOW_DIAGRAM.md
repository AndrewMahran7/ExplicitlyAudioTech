# Explicitly Web Interface - Processing Flow

## 📊 How It Works

```
┌─────────────────────────────────────────────────────────────────┐
│                          USER INTERFACE                          │
└─────────────────────────────────────────────────────────────────┘
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│  Step 1: UPLOAD FILE                                            │
│  • User selects MP3/WAV/FLAC file                              │
│  • Drag & drop or click to browse                              │
│  • Max 100MB, validated on client                              │
└─────────────────────────────────────────────────────────────────┘
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│  Step 2: SELECT OPTIONS                                         │
│  • Model: Speed / Balanced / Accuracy                          │
│  • Method: Mute / Bleep / Reverse                              │
│  • Device: Auto / GPU / CPU                                     │
└─────────────────────────────────────────────────────────────────┘
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│  Step 3: SUBMIT & CREATE JOB                                    │
│  • POST to /upload endpoint                                     │
│  • Generate unique job ID (UUID)                               │
│  • Save file to data/uploads/[job_id]/                         │
│  • Create job entry in memory                                   │
└─────────────────────────────────────────────────────────────────┘
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│  Step 4: START BACKGROUND THREAD                                │
│  • Thread created for processing                                │
│  • Main server continues accepting requests                     │
│  • Job status: "queued" → "processing"                         │
└─────────────────────────────────────────────────────────────────┘
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│  Step 5: SEPARATE AUDIO STEMS (Demucs)                         │
│  Progress: 0% → 30%                                            │
│  • Load audio file                                              │
│  • Run Demucs model (htdemucs or htdemucs_ft)                  │
│  • Extract vocals track                                         │
│  • Extract instrumental track                                   │
│  • Save to data/stems/[job_id]/                                │
└─────────────────────────────────────────────────────────────────┘
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│  Step 6: TRANSCRIBE SPEECH (stable-ts + Whisper)              │
│  Progress: 30% → 40%                                           │
│  • Load vocals audio                                            │
│  • Run stable-ts with Whisper model (small/tiny/large)        │
│  • Apply Voice Activity Detection (VAD)                        │
│  • Get highly accurate word-level timestamps                   │
│  • Extract confidence scores                                    │
│  • Fallback to standard Whisper if stable-ts unavailable      │
│  • Log all transcribed words                                    │
└─────────────────────────────────────────────────────────────────┘
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│  Step 6.5: ALIGN WITH LYRICS (Optional)                        │
│  Progress: 40% → 50%                                           │
│  • IF lyrics provided by user:                                 │
│    - Compare transcription with actual lyrics                  │
│    - Calculate accuracy percentage                              │
│    - Identify misheard words (differences)                     │
│    - Correct words using sequence matching                     │
│    - Preserve original timestamps                              │
│    - Log corrections made                                       │
│  • ELSE: Skip this step                                        │
└─────────────────────────────────────────────────────────────────┘
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│  Step 7: DETECT PROFANITY (Lexicon Match)                      │
│  Progress: 50% → 70%                                           │
│  • Load profanity lexicon                                       │
│  • Normalize words (sh*t → shit)                               │
│  • Match against transcribed words                              │
│  • Filter by confidence threshold                               │
│  • Log detected profanity with timestamps                       │
└─────────────────────────────────────────────────────────────────┘
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│  Step 8: CENSOR AUDIO (AudioCensor)                            │
│  Progress: 70% → 85%                                           │
│  • Load original vocals                                         │
│  • For each profane segment:                                    │
│    - Apply pre/post margins                                     │
│    - Apply chosen method:                                       │
│      * Mute: Silence with fade                                 │
│      * Bleep: Replace with tone                                │
│      * Reverse: Play backward                                  │
│  • Save censored vocals to data/work/[job_id]/                │
└─────────────────────────────────────────────────────────────────┘
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│  Step 9: REMIX AUDIO                                            │
│  Progress: 85% → 95%                                           │
│  • Load censored vocals                                         │
│  • Load instrumental track                                      │
│  • Mix at original volume levels                               │
│  • Export as WAV to data/output/[job_id]/                     │
└─────────────────────────────────────────────────────────────────┘
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│  Step 10: GENERATE REPORT                                       │
│  Progress: 95% → 100%                                          │
│  • Compile processing statistics                               │
│  • List all detected profane words                             │
│  • Include timestamps and confidence                            │
│  • Save JSON report to data/output/[job_id]/                  │
└─────────────────────────────────────────────────────────────────┘
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│  Step 11: CLEANUP & COMPLETE                                    │
│  • Delete temporary stems                                       │
│  • Delete work files                                            │
│  • Keep only output files                                       │
│  • Update job status: "completed"                              │
└─────────────────────────────────────────────────────────────────┘
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│  Step 12: DOWNLOAD RESULTS                                      │
│  • User downloads clean audio file                             │
│  • User downloads JSON report (optional)                        │
│  • User can process another file                               │
└─────────────────────────────────────────────────────────────────┘


## 🔄 Frontend Status Polling

While processing runs in background thread:

```
┌─────────────────────────────┐
│   JavaScript Frontend       │
│   (in browser)              │
└─────────────────────────────┘
            ▼
    Every 1 second:
            ▼
┌─────────────────────────────┐
│  GET /status/[job_id]       │
└─────────────────────────────┘
            ▼
┌─────────────────────────────┐
│  Flask Backend              │
│  Returns job status:        │
│  {                          │
│    status: "processing",    │
│    step: "Transcribing      │
│           speech with       │
│           stable-ts...",    │
│    progress: 45             │
│  }                          │
└─────────────────────────────┘
            ▼
┌─────────────────────────────┐
│  Update UI:                 │
│  • Progress bar: 45%        │
│  • Status: "Transcribing    │
│    with stable-ts"          │
└─────────────────────────────┘
            ▼
    Status == "completed"?
            ▼
┌─────────────────────────────┐
│  Show Results Page          │
│  • Download buttons         │
│  • Statistics               │
│  • Timestamp accuracy info  │
└─────────────────────────────┘
```

## 🧵 Threading Model

```
Main Flask Thread
  ├── Handles HTTP requests
  ├── Serves static files
  └── Manages job status

Background Worker Threads (one per upload)
  ├── Process audio file
  ├── Update job status
  └── Exit when complete
```

## 📁 File Structure During Processing

```
data/
├── uploads/
│   └── [job-uuid]/
│       └── original.mp3          ← Uploaded file
│
├── stems/
│   └── [job-uuid]/
│       ├── vocals.wav             ← From Demucs
│       └── instrumental.wav       ← From Demucs
│
├── work/
│   └── [job-uuid]/
│       └── censored_vocals.wav    ← After censoring
│
└── output/
    └── [job-uuid]/
        ├── original.clean.wav     ← Final output ✅
        └── original.report.json   ← Final report ✅
```

After completion, stems/ and work/ directories are deleted.

## ⚙️ Model Selection Impact
Speed Model (tiny + stable-ts)
  ├── Pros: Fast processing, low memory, accurate timestamps
  ├── Cons: May miss some words in noisy audio
  └── Use: Testing, long files, quick processing

Balanced Model (small + stable-ts)
  ├── Pros: Excellent accuracy, good speed, precise timestamps
  ├── Cons: Moderate resources (2GB VRAM)
  └── Use: Most use cases ⭐ (recommended)

Accuracy Model (large-v2 + stable-ts)
  ├── Pros: Best accuracy, perfect timestamps, catches everything
  ├── Cons: Slow, high memory usage (10GB VRAM)
  └── Use: Professional content, critical accuracy needshing
  ├── Cons: Slow, high memory usage
  └── Use: Important content, short files
```

## 🎯 API Endpoints

```
GET  /                      → HTML interface
POST /upload                → Upload file, start processing
GET  /status/<job_id>       → Get job status (polling)
GET  /download/<job_id>     → Download clean audio
GET  /report/<job_id>       → Download JSON report
GET  /static/style.css      → CSS stylesheet
GET  /static/script.js      → JavaScript code
```

## 🔐 Job ID Security

```
Job ID = UUID4 (e.g., "a3f7c2d5-8b1e-4a9f-b3c7-1d5e8a2f9b4c")

Properties:
  ✅ Unique (collision chance: ~1 in 5.3×10^36)
  ✅ Unpredictable (can't guess other job IDs)
  ✅ No sequential pattern (no enumeration)
  ⚠️  Not authenticated (anyone with ID can access)
```

For production, add authentication layer.

## 📊 Performance Metrics

```
CPU (Intel i7):
├── Separation: ~8 min
├── Transcription (small + stable-ts): ~6 min
├── Detection: <1 sec
├── Censoring: ~1 min
└── Total: ~16 minutes

GPU (RTX 3060):
├── Separation: ~1 min
├── Transcription (small + stable-ts): ~40 sec
├── Detection: <1 sec
├── Censoring: ~30 sec
└── Total: ~2.5-3 minutes

Speed up: ~5-7x with GPU
Note: stable-ts adds ~20% processing time but provides ±30-80ms timestamp accuracy vs ±150-250ms with standard Whisper

Speed up: ~5-7x with GPU
```

## 🎨 User Experience Flow

```
                    Landing Page
                        │
                        ▼
              ┌─────────────────┐
              │ Select & Upload │
              └─────────────────┘
                        │
                        ▼
              ┌─────────────────┐
              │   Processing    │
              │   [Progress]    │
              └─────────────────┘
                        │
                ┌───────┴───────┐
                ▼               ▼
         ┌──────────┐    ┌──────────┐
         │ Success! │    │  Error   │
         │ Download │    │ Try Again│
         └──────────┘    └──────────┘
                │
                ▼
This flow ensures users always know:
- What's happening
- How long it will take
- What to do next

## 🎯 Transcription Pipeline (stable-ts Integration)

```
┌─────────────────────────────────────────────────────────────┐
│  Audio Input (vocals.wav)                                   │
└─────────────────────────────────────────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  Check Dependencies                                          │
│  • stable-ts installed?                                     │
│  • PyTorch available?                                       │
│  • CUDA available?                                          │
└─────────────────────────────────────────────────────────────┘
                         ▼
                  ┌──────┴──────┐
                  ▼             ▼
    ┌──────────────────┐  ┌──────────────────┐
    │  stable-ts Path  │  │  Fallback Path   │
    │  (Preferred)     │  │  (Standard)      │
    └──────────────────┘  └──────────────────┘
            ▼                      ▼
┌─────────────────────┐  ┌─────────────────────┐
│  Load Whisper Model │  │  Load Whisper Model │
│  via stable-ts      │  │  Standard API       │
└─────────────────────┘  └─────────────────────┘
            ▼                      ▼
┌─────────────────────┐  ┌─────────────────────┐
│  Transcribe with:   │  │  Transcribe with:   │
│  • VAD enabled      │  │  • Basic timestamps │
│  • Word timestamps  │  │  • Word timestamps  │
│  • High precision   │  │  • Standard quality │
└─────────────────────┘  └─────────────────────┘
            ▼                      ▼
┌─────────────────────┐  ┌─────────────────────┐
│  Convert to         │  │  Already in         │
│  WordSegment objects│  │  WordSegment format │
└─────────────────────┘  └─────────────────────┘
            │                      │
            └──────────┬───────────┘
                       ▼
        ┌──────────────────────────┐
        │  Unified WordSegment[]   │
        │  Ready for detection     │
        └──────────────────────────┘
                       ▼
        ┌──────────────────────────┐
        │  Profanity Detection     │
        └──────────────────────────┘
```

## 📦 Dependencies Overview

```
Core Processing:
├── stable-ts (optional, recommended)
│   └── Provides: Enhanced timestamp accuracy
├── openai-whisper (required)
│   └── Provides: Speech-to-text transcription
├── torch (required)
│   └── Provides: Neural network backend
├── demucs (required)
│   └── Provides: Audio stem separation
└── soundfile (required)
    └── Provides: Audio I/O operations

Graceful Degradation:
├── stable-ts missing → Use standard Whisper
├── CUDA unavailable → Use CPU processing
└── System maintains full functionality

This flow ensures users always know:
- What's happening
- How long it will take
- What to do next
